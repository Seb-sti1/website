import {FC} from "react";
import "./error.css";

const UnknownError: FC = () => {

    return (
        <div id="error">
            <p>Unknown error.</p>
        </div>
    );
};

export default UnknownError;
